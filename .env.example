# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL=
CORS_ORIGINS=
# Comma-separated IPs/CIDRs allowed to set X-Forwarded-For (see main.ts's
# `trust proxy` config). Defaults to the standard RFC1918 private ranges if unset.
TRUSTED_PROXIES=
# Required. Secret used to sign/verify JWTs (@nestjs/jwt) — at least 32 characters.
JWT_SECRET=
# Optional. Token lifetime, e.g. "2h", "7d", or a plain number of seconds. Defaults to "2h".
JWT_EXPIRES_IN=
